{
  "name": "nmcanvas",
  "version": "1.0.0",
  "type": "module",
  "description": "canvas interface for router",
  "main": "index.js",
  "scripts": {
    "validate:schema": "ajv validate -s schema/canonical-graph.schema.json -d model/*.json",
    "test:canvas": "node dist/model/canvasContract.js",
    "full:test": "rm -rf dist && npx tsc && npm run test:canvas"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.10.0",
    "ajv": "^8.17.1",
    "ajv-cli": "^5.0.0",
    "ajv-dist": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.9.3"
  }
}
